Noto Serif JP — the overlay's serif/mincho face (issue #84)
===========================================================

Bundled as   : app/src/main/assets/fonts/NotoSerifJP-Regular.ttf
               8,079,912 bytes, sha256
               f5da1e80a3e24a56eacfd10091e1a6448ac6118e641bda25f0d1e8e82b3010cd
               17,923 glyphs — the whole face, nothing subset (the overlay
               draws dictionary text of arbitrary breadth).
Used by      : the OCR overlay's "serif" face — the LineOverlayView glyphs over
               the screenshot plus every TextView the overlay builds (dictionary
               panel, alternatives, neighbour chips, pitch line), selected by
               the "Serif font (mincho) in the OCR overlay" setting (#84).
Copyright    : (c) 2017-2024 Adobe (http://www.adobe.com/). The OFL.txt
               distributed beside the upstream file additionally carries
               "Copyright 2012 Google Inc. All Rights Reserved."; both are
               reproduced here because the binary and the upstream licence copy
               name different holders.
Licence      : SIL Open Font License, Version 1.1 (OFL-1.1).
Licence text : licenses/texts/ofl-1.1.txt — the official OFL text as published
               at openfontlicense.org, fetched 2026-09-14 (4,599 bytes, sha256
               1d361a8f8e8ce6e68457dcd93fb56e162e6baa3bbb7e7573a290d44399f6b57e).

Where the font came from
------------------------

  Source    : https://github.com/google/fonts — ofl/notoserifjp/NotoSerifJP[wght].ttf
  Retrieved : 2026-09-14. The local copy is byte-identical to that file:
              13,574,352 bytes, sha256
              2fd527ba12b6a44ec30d796d633360da0aeba6c5d4af1304ce12bb4dc15a7dfc
  Upstream  : https://github.com/notofonts/noto-cjk (Adobe/Google Noto)
  Version   : 2.003 (H1). The variable font's fvar default is wght=200
              (ExtraLight).

Modification: static Regular instance
-------------------------------------

The variable font cannot ship as-is — its default instance is ExtraLight, which
would render a hairline face — and shipping the variable font would put 13.6 MB
per face into the APK. The bundled file is a static instance at wght=400
produced with fontTools:

  fontTools.varLib.instancer.instantiateVariableFont(font, {"wght": 400},
      updateFontNames=True)

That is the only modification: the outlines are the variable font's Regular
master, the fvar/gvar/HVAR/avar tables are dropped, and the name table says
"Noto Serif JP / Regular". The result is distributed under the same OFL-1.1.
This face declares no Reserved Font Name.

Vertical substitution verified (issue #47)
------------------------------------------

The overlay renders vertical lines with Paint.fontFeatureSettings = "'vert' 1",
so the face must carry the vertical GSUB features. Checked with fontTools on the
committed file:

  vert  : lookups 16/17/18, single substitutions covering 378 + 1 + 4 glyphs
          (、→uniFE11, 。→uniFE12, 「→uniFE41, 」→uniFE42, （→uniFE35,
          ）→uniFE36, ー→glyph17777)
  vrt2  : lookup 16, covering the same 378 glyphs
  scripts: both registered under DFLT, cyrl, grek, hani, kana and latn
          (default and JAN langsys); vhea/vmtx vertical metrics present.

The JVM test OverlayFontTest parses this committed file and fails if vert or
vrt2 disappears, so a future font swap cannot silently drop the substitution.
